"""Auto-generated by fcstd2b123d from tests/fixtures/sample_2026/Sprocket_ISO606_simplex_6x2_8_z26.FCStd."""

from build123d import (
    Axis,
    CenterArc,
    Circle,
    Plane,
    Polyline,
    Rot,
    Sketch,
    extrude,
    make_face,
    revolve,
)


def _pattern_union(base, *additions):
    """Boolean-union ``base`` with each addition via BuildPart.

    Chained ``+`` on build123d Part objects returns a Compound that does not
    fuse overlapping geometry — so for pattern features whose copies overlap
    (e.g. sprocket teeth meeting at the hub) the resulting volume is wrong.
    Routing through BuildPart.add() invokes OCCT's robust boolean fusion.
    """
    from build123d import BuildPart, add

    with BuildPart() as _bp:
        add(base)
        for s in additions:
            add(s)
    return _bp.part


def make_part(Wt=2.6):
    """Translated parametric design. Defaults match the source values."""
    # Sketcher::SketchObject 'Tooth sketch': 6 lines, 5 arcs (1 loops)
    tooth_sketch_profile = (
        CenterArc(
            center=(-3, 24.703506330073875),
            radius=2,
            start_angle=276.92410835900756,
            arc_size=83.07589164099255,
        )
        + CenterArc(
            center=(12.439999999999994, 24.703506330073896),
            radius=13.44,
            start_angle=180,
            arc_size=-5.678086191357352,
        )
        + CenterArc(
            center=(0, 0),
            radius=26.049999999999997,
            start_angle=92.0548547082139,
            arc_size=-4.109709416427748,
        )
        + CenterArc(
            center=(-12.440000000000007, 24.703506330073893),
            radius=13.44,
            start_angle=5.678086191357296,
            arc_size=-5.678086191357346,
        )
        + CenterArc(
            center=(3, 24.703506330073875),
            radius=2,
            start_angle=180,
            arc_size=83.07589164099252,
        )
        + Polyline(
            (2.7588908981313987, 22.71809292199078),
            (2.7588908981313987, 0),
            (-2.7588908981314106, 0),
            (-2.7588908981314106, 22.718092921990785),
        )
    )
    tooth_sketch = Sketch() + Plane(
        origin=(0, 0, 0), x_dir=(1, 0, 0), z_dir=(0, -1, 0)
    ) * (make_face(tooth_sketch_profile))

    # PartDesign::Pad 'Tooth': length=Wt
    tooth = extrude(tooth_sketch, amount=Wt)

    # Sketcher::SketchObject 'Hub sketch': 1 circle (1 loops)
    hub_sketch = Sketch() + Plane(
        origin=(0, 0, 0), x_dir=(1, 0, 0), z_dir=(0, -1, 0)
    ) * (Circle(15))

    # Sketcher::SketchObject 'Hole sketch': 1 circle (1 loops)
    hole_sketch = Sketch() + Plane(
        origin=(0, 0, 0), x_dir=(1, 0, 0), z_dir=(0, -1, 0)
    ) * (Circle(4))

    # PartDesign::PolarPattern 'Teeth': PolarPattern around (0, 1, 0), step=13.846153846153847deg, occurrences=26
    teeth = _pattern_union(
        tooth,
        Rot(Y=13.846153846153847) * extrude(tooth_sketch, amount=Wt),
        Rot(Y=27.692307692307693) * extrude(tooth_sketch, amount=Wt),
        Rot(Y=41.53846153846154) * extrude(tooth_sketch, amount=Wt),
        Rot(Y=55.38461538461539) * extrude(tooth_sketch, amount=Wt),
        Rot(Y=69.23076923076923) * extrude(tooth_sketch, amount=Wt),
        Rot(Y=83.07692307692308) * extrude(tooth_sketch, amount=Wt),
        Rot(Y=96.92307692307693) * extrude(tooth_sketch, amount=Wt),
        Rot(Y=110.76923076923077) * extrude(tooth_sketch, amount=Wt),
        Rot(Y=124.61538461538461) * extrude(tooth_sketch, amount=Wt),
        Rot(Y=138.46153846153845) * extrude(tooth_sketch, amount=Wt),
        Rot(Y=152.30769230769232) * extrude(tooth_sketch, amount=Wt),
        Rot(Y=166.15384615384616) * extrude(tooth_sketch, amount=Wt),
        Rot(Y=180) * extrude(tooth_sketch, amount=Wt),
        Rot(Y=193.84615384615387) * extrude(tooth_sketch, amount=Wt),
        Rot(Y=207.6923076923077) * extrude(tooth_sketch, amount=Wt),
        Rot(Y=221.53846153846155) * extrude(tooth_sketch, amount=Wt),
        Rot(Y=235.3846153846154) * extrude(tooth_sketch, amount=Wt),
        Rot(Y=249.23076923076923) * extrude(tooth_sketch, amount=Wt),
        Rot(Y=263.0769230769231) * extrude(tooth_sketch, amount=Wt),
        Rot(Y=276.9230769230769) * extrude(tooth_sketch, amount=Wt),
        Rot(Y=290.7692307692308) * extrude(tooth_sketch, amount=Wt),
        Rot(Y=304.61538461538464) * extrude(tooth_sketch, amount=Wt),
        Rot(Y=318.46153846153845) * extrude(tooth_sketch, amount=Wt),
        Rot(Y=332.3076923076923) * extrude(tooth_sketch, amount=Wt),
        Rot(Y=346.1538461538462) * extrude(tooth_sketch, amount=Wt),
    )

    # Sketcher::SketchObject 'Radius': 5 lines, 2 arcs (2 loops)
    sketch_001_loop_0 = CenterArc(
        center=(3.4000000000000012, 23.23752777791496),
        radius=6,
        start_angle=152.0471134108649,
        arc_size=27.953193868257955,
    ) + Polyline(
        (-2.5999999999137122, 23.23749559972046),
        (-2.6, 26.05),
        (-1.9000000000000012, 26.05),
    )
    sketch_001_loop_1 = Polyline(
        (-0.6999999999999993, 26.05), (0, 26.05), (0, 23.237527777914956)
    ) + CenterArc(
        center=(-6, 23.237527777914956),
        radius=6,
        start_angle=0,
        arc_size=27.952886589135115,
    )
    radius = Sketch() + Plane(origin=(0, 0, 0), x_dir=(0, 1, 0), z_dir=(1, 0, 0)) * (
        (make_face(sketch_001_loop_0) + make_face(sketch_001_loop_1))
    )

    # PartDesign::Groove 'Teeth Radius': angle=-360.0 (reversed)
    teeth_radius = teeth - revolve(radius, axis=Axis.Y, revolution_arc=-360)

    # PartDesign::Pad 'Hub': length=15.0
    hub = teeth_radius + extrude(hub_sketch, amount=15)

    # PartDesign::Pocket 'Hole': ThroughAll (midplane)
    hole = hub - extrude(hole_sketch, amount=500000, both=True)

    return hole


result = make_part()
