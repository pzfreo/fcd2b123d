"""Auto-generated by fcstd2b123d from tests/fixtures/sample_813/Sprocket_ISO606_simplex_6x2_8_z30.FCStd."""

from build123d import (
    Axis,
    CenterArc,
    Circle,
    Plane,
    Polyline,
    Rot,
    Sketch,
    extrude,
    make_face,
    revolve,
)


def _pattern_union(base, *additions):
    """Boolean-union ``base`` with each addition via BuildPart.

    Chained ``+`` on build123d Part objects returns a Compound that does not
    fuse overlapping geometry — so for pattern features whose copies overlap
    (e.g. sprocket teeth meeting at the hub) the resulting volume is wrong.
    Routing through BuildPart.add() invokes OCCT's robust boolean fusion.
    """
    from build123d import BuildPart, add

    with BuildPart() as _bp:
        add(base)
        for s in additions:
            add(s)
    return _bp.part


def make_part(Wt=2.6):
    """Translated parametric design. Defaults match the source values."""
    # Sketcher::SketchObject 'Tooth sketch': 6 lines, 5 arcs (1 loops)
    sketch_profile = (
        CenterArc(
            center=(-3, 28.552829982332753),
            radius=2,
            start_angle=275.9979688957663,
            arc_size=84.00203110423382,
        )
        + CenterArc(
            center=(14.360000000000001, 28.552829982332785),
            radius=15.36,
            start_angle=180,
            arc_size=-4.9760803906785895,
        )
        + CenterArc(
            center=(0, 0),
            radius=29.899999999999995,
            start_angle=91.80561050655027,
            arc_size=-3.6112210131005327,
        )
        + CenterArc(
            center=(-14.36, 28.55282998233278),
            radius=15.36,
            start_angle=4.976080390678484,
            arc_size=-4.976080390678586,
        )
        + CenterArc(
            center=(3, 28.552829982332753),
            radius=2,
            start_angle=180,
            arc_size=84.00203110423382,
        )
        + Polyline(
            (2.7910135841170334, 26.5637787818916),
            (2.7910135841170334, 0),
            (-2.791013584117032, 0),
            (-2.791013584117032, 26.5637787818916),
        )
    )
    sketch = Sketch() + Plane(origin=(0, 0, 0), x_dir=(1, 0, 0), z_dir=(0, -1, 0)) * (
        make_face(sketch_profile)
    )

    # PartDesign::Pad 'Tooth': length=Wt
    pad = extrude(sketch, amount=Wt)

    # Sketcher::SketchObject 'Hub sketch': 1 circle (1 loops)
    sketch_002 = Sketch() + Plane(
        origin=(0, 0, 0), x_dir=(1, 0, 0), z_dir=(0, -1, 0)
    ) * (Circle(15))

    # Sketcher::SketchObject 'Hole sketch': 1 circle (1 loops)
    sketch_003 = Sketch() + Plane(
        origin=(0, 0, 0), x_dir=(1, 0, 0), z_dir=(0, -1, 0)
    ) * (Circle(4))

    # PartDesign::PolarPattern 'Teeth': PolarPattern around (0, 1, 0), step=12.0deg, occurrences=30
    polar_pattern = _pattern_union(
        pad,
        Rot(Y=12) * extrude(sketch, amount=Wt),
        Rot(Y=24) * extrude(sketch, amount=Wt),
        Rot(Y=36) * extrude(sketch, amount=Wt),
        Rot(Y=48) * extrude(sketch, amount=Wt),
        Rot(Y=60) * extrude(sketch, amount=Wt),
        Rot(Y=72) * extrude(sketch, amount=Wt),
        Rot(Y=84) * extrude(sketch, amount=Wt),
        Rot(Y=96) * extrude(sketch, amount=Wt),
        Rot(Y=108) * extrude(sketch, amount=Wt),
        Rot(Y=120) * extrude(sketch, amount=Wt),
        Rot(Y=132) * extrude(sketch, amount=Wt),
        Rot(Y=144) * extrude(sketch, amount=Wt),
        Rot(Y=156) * extrude(sketch, amount=Wt),
        Rot(Y=168) * extrude(sketch, amount=Wt),
        Rot(Y=180) * extrude(sketch, amount=Wt),
        Rot(Y=192) * extrude(sketch, amount=Wt),
        Rot(Y=204) * extrude(sketch, amount=Wt),
        Rot(Y=216) * extrude(sketch, amount=Wt),
        Rot(Y=228) * extrude(sketch, amount=Wt),
        Rot(Y=240) * extrude(sketch, amount=Wt),
        Rot(Y=252) * extrude(sketch, amount=Wt),
        Rot(Y=264) * extrude(sketch, amount=Wt),
        Rot(Y=276) * extrude(sketch, amount=Wt),
        Rot(Y=288) * extrude(sketch, amount=Wt),
        Rot(Y=300) * extrude(sketch, amount=Wt),
        Rot(Y=312) * extrude(sketch, amount=Wt),
        Rot(Y=324) * extrude(sketch, amount=Wt),
        Rot(Y=336) * extrude(sketch, amount=Wt),
        Rot(Y=348) * extrude(sketch, amount=Wt),
    )

    # Sketcher::SketchObject 'Radius': 5 lines, 2 arcs (2 loops)
    sketch_001_loop_0 = CenterArc(
        center=(3.400000000000002, 27.087527777914953),
        radius=6,
        start_angle=152.0471134108649,
        arc_size=27.95305807997329,
    ) + Polyline((-2.6, 27.087509819436374), (-2.6, 29.9), (-1.9000000000000001, 29.9))
    sketch_001_loop_1 = Polyline(
        (-0.7, 29.9), (0, 29.9), (0, 27.087509819436374)
    ) + CenterArc(
        center=(-6, 27.087527777914957),
        radius=6,
        start_angle=359.9998285091618,
        arc_size=27.953058079973346,
    )
    sketch_001 = Sketch() + Plane(
        origin=(0, 0, 0), x_dir=(0, 1, 0), z_dir=(1, 0, 0)
    ) * ((make_face(sketch_001_loop_0) + make_face(sketch_001_loop_1)))

    # PartDesign::Groove 'Teeth Radius': angle=-360.0 (reversed)
    groove = polar_pattern - revolve(sketch_001, axis=Axis.Y, revolution_arc=-360)

    # PartDesign::Pad 'Hub': length=15.0
    pad_001 = groove + extrude(sketch_002, amount=15)

    # PartDesign::Pocket 'Hole': ThroughAll (midplane)
    pocket = pad_001 - extrude(sketch_003, amount=500000, both=True)

    return pocket


result = make_part()
